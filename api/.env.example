PORT=3333
NODE_ENV=production

# Dominios permitidos para consumir a API.
# Na VPS, use o dominio real do front na Vercel.
# Para liberar preview deployments, adicione os dominios separados por virgula.
CORS_ORIGIN=https://seu-front.vercel.app,https://www.seu-dominio.com.br

DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=crm_thiago_adv
DB_USER=crm_api_user
DB_PASSWORD=troque-esta-senha
DB_SSL=false
